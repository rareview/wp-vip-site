{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Listen for XDebug",
      "type": "php",
      "request": "launch",
      "port": 9090,
      "pathMappings": {
        "/app/.local/site/wp-content": "${workspaceFolder}"
      }
    }
  ]
}
